data-sketch: 1.0.0-draft.2
info:
  name: online-shop

sources:
  openapi: ../../tables-doc/fixtures/openapi/openapi.yaml

claims:
  customer:
    name: customers
    reason: Customer profile information is needed when orders are created.
    traces:
      operations:
        - createCustomer
    details:
      - name
