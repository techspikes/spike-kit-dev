data-sketch: 1.0.0-draft.1

info:
  name: online-shop

sources:
  openapi: ./openapi/openapi-invalid-syntax.yaml

stores:
  customer:
    name: customers
    traces:
      operations:
        - createCustomer
    reason: Customer profiles need to be looked up when handling orders and support requests.
    fields:
      id:
        name: id
        type:
          name: integer
        nullable: false
